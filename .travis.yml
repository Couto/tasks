language: node_js
node_js:
  - "node"

before_install:
  - "npm install elm@0.18.0"

script: elm make --yes --warn Main.elm

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master
